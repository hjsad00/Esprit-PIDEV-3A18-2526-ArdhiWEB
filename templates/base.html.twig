<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">
  <title>{% block title %}Ardhi - Agriculture{% endblock %}</title>

  <!-- Favicons -->
  <link href="{{ asset('assets/img/ardhi_logo.png') }}" rel="icon">
  <link href="{{ asset('assets/img/ardhi_logo.png') }}" rel="apple-touch-icon">

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com" rel="preconnect">
  <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Open+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,300;1,400;1,500;1,600;1,700;1,800&family=Marcellus:wght@400&display=swap" rel="stylesheet">

  <!-- Vendor CSS Files -->
  <link href="{{ asset('assets/vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
  <link href="{{ asset('assets/vendor/bootstrap-icons/bootstrap-icons.css') }}" rel="stylesheet">
  <link href="{{ asset('assets/vendor/aos/aos.css') }}" rel="stylesheet">
  <link href="{{ asset('assets/vendor/swiper/swiper-bundle.min.css') }}" rel="stylesheet">
  <link href="{{ asset('assets/vendor/glightbox/css/glightbox.min.css') }}" rel="stylesheet">

  <!-- Main CSS File -->
  <link href="{{ asset('assets/css/main.css') }}" rel="stylesheet">
  
  {% block stylesheets %}{% endblock %}
</head>

  {% set current_path = app.request.pathInfo %}
  {% set current_route = app.request.attributes.get('_route') %}
  {% set current_controller = app.request.attributes.get('_controller') %}

  {% set is_user_diag_route = 
    (current_path starts with '/user-and-diag' and not (current_path starts with '/admin')) or 
    (current_route starts with 'app_user_and_diag_') or 
    (current_route in ['app_client_dashboard', 'app_profile', 'app_login', 'app_register']) or
    ('Controller\\UserAndDiag' in current_controller and 'Controller\\UserAndDiag\\Admin' not in current_controller)
  %}

{% set is_homepage = current_route == 'app_home' %}
{% set is_premium = is_homepage or is_user_diag_route %}
<body class="{{ is_premium ? 'premium-header-active' : '' }} {{ is_homepage ? 'index-page' : '' }} {% block body_class %}{% endblock %}" {% if is_user_diag_route %}style="background: url('{{ asset('assets/img/DiagComm_bg.png') }}') no-repeat center center fixed; background-size: cover;"{% endif %}>

  <div class="flash-container">
    {% for label, messages in app.flashes %}
      {% for message in messages %}
        <div class="premium-alert alert-{{ label }} alert-dismissible fade show" role="alert">
          {% if label == 'success' %}
            <i class="bi bi-check-circle-fill"></i>
          {% elseif label == 'danger' %}
            <i class="bi bi-exclamation-triangle-fill"></i>
          {% else %}
            <i class="bi bi-info-circle-fill"></i>
          {% endif %}
          <span>{{ message }}</span>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      {% endfor %}
    {% endfor %}
  </div>

  <header id="header" class="header d-flex align-items-center fixed-top {% block header_class %}{% endblock %}">
    <div class="container-fluid container-xl position-relative d-flex align-items-center justify-content-between">

      {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('admin_agriculteurs_employe') }}" class="logo d-flex align-items-center">
      {% else %}
        <a href="/" class="logo d-flex align-items-center">
      {% endif %}
        <img src="{{ asset('assets/img/ardhi_logo.png') }}" alt="Ardhi" style="max-height: 50px;">
      </a>

      <nav id="navmenu" class="navmenu">
        <ul>
          <li><a href="{{ path('app_client_dashboard') }}">Diagnostics IA</a></li>
          <li><a href="#">Marketplace</a></li>
          <li><a href="#">Maintenance</a></li>
          <li><a href="#">Cultures</a></li>
          <li><a href="#">Évènements</a></li>
          {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_AGRICULTEUR') %}
          <li>
           <a href="{{ path('app_employe_dashboard') }}"
            class="{{ app.request.get('_route') == 'app_employe_dashboard' ? 'active' : '' }}">
            Employés
          </a>
          </li>
          {% endif %}
          
          {% if app.user %}
          <li class="dropdown"><a href="#" class="d-flex align-items-center px-2">
            <div class="nav-user-circle">
              <i class="bi bi-person-fill" style="font-size: 1.3rem;"></i>
            </div></a>
            <ul>
              <li><a href="{{ path('app_profile') }}">Mon Profil {{ app.user.prenom ? ('(' ~ app.user.prenom ~ ')') : '' }}</a></li>
              <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
            </ul>
          </li>
          {% else %}
          <li><a href="{{ path('app_login') }}" class="nav-cta-btn">Se connecter</a></li>
          {% endif %}
        </ul>
        <i class="mobile-nav-toggle d-xl-none bi bi-list"></i>
      </nav>

    </div>
  </header>

  <main class="main" style="min-height: 70vh;">
      {% block body %}{% endblock %}
  </main>

  <footer id="footer" class="footer dark-background">
    <div class="footer-top">
      <div class="container">
        <div class="row gy-4">
          <div class="col-lg-4 col-md-6 footer-about">
            <a href="/" class="logo d-flex align-items-center">
              <span class="sitename">Ardhi</span>
            </a>
            <div class="footer-contact pt-3">
              <p>Votre plateforme d'agriculture intelligente</p>
              <p>Tunisie</p>
              <p class="mt-3"><strong>Email:</strong> <span>contact@ardhi.com</span></p>
            </div>
          </div>

          <div class="col-lg-2 col-md-3 footer-links">
            <h4>Liens utiles</h4>
            <ul>
              {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('admin_agriculteurs_employe') }}">Accueil</a></li>
              {% else %}
                <li><a href="/">Accueil</a></li>
              {% endif %}
              <li><a href="#">À propos</a></li>
              <li><a href="#">Services</a></li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="copyright text-center">
      <div class="container d-flex flex-column flex-lg-row justify-content-center justify-content-lg-between align-items-center">
        <div class="d-flex flex-column align-items-center align-items-lg-start">
          <div>
            © Copyright <strong><span>Ardhi</span></strong>. Tous droits réservés
          </div>
        </div>
      </div>
    </div>
  </footer>

  <!-- Scroll Top -->
  <a href="#" id="scroll-top" class="scroll-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>


  <!-- Vendor JS Files -->
  <script src="{{ asset('assets/vendor/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
  <script src="{{ asset('assets/vendor/php-email-form/validate.js') }}"></script>
  <script src="{{ asset('assets/vendor/aos/aos.js') }}"></script>
  <script src="{{ asset('assets/vendor/swiper/swiper-bundle.min.js') }}"></script>
  <script src="{{ asset('assets/vendor/glightbox/js/glightbox.min.js') }}"></script>

  <!-- Premium Flash Auto-dismiss -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(() => {
        document.querySelectorAll('.premium-alert').forEach(alert => {
          const bsAlert = new bootstrap.Alert(alert);
          bsAlert.close();
        });
      }, 5000);
    });
  </script>

  <!-- Main JS File -->
  <script src="{{ asset('assets/js/main.js') }}"></script>

  {% block javascripts %}
    {{ importmap('app') }}
  {% endblock %}

</body>
</html>
