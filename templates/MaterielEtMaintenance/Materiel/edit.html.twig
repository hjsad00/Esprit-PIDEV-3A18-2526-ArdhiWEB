{% extends 'base.html.twig' %}

{% block title %}Modifier le matériel{% endblock %}

{% block body_class %}materiel-theme{% endblock %}

{% block stylesheets %}
    <link href="{{ asset('assets/css/materiel/materiel.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
<div class="materiel-page-wrapper">
    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-lg" style="border-radius: 20px; border: none; overflow: hidden;">
                    <div class="card-header text-dark" style="background: linear-gradient(135deg, #feca57 0%, #ff9f43 100%); padding: 25px 30px;">
                        <h4 class="mb-0 fw-bold"><i class="bi bi-pencil-square me-2"></i>Modifier: {{ materiel.nom }}</h4>
                    </div>
                    <div class="card-body p-5">
                        {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <div class="d-flex justify-content-between mt-5 pt-3 border-top">
                                <a href="{{ path('app_materiel_index') }}" class="btn-mat-outline px-4"><i class="bi bi-arrow-left"></i> Annuler</a>
                                <button class="btn-mat-primary px-5" type="submit" style="background: #e67e22;"><i class="bi bi-save"></i> Enregistrer les modifications</button>
                            </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
